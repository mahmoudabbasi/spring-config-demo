spring:
  application:
    name: client-service-b   # یا service-b
  config:
    import: "configserver:http://localhost:8888"
  kafka:
    bootstrap-servers: localhost:9092

management:
  endpoints:
    web:
      exposure:
        include: "bus-refresh,refresh,health,info"